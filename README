docker-chromedriver
~~~~~~~~~~~~~~~~~~~

* Usage

  $ docker run --net=host -t .../chromedriver:stable /cd
  $ docker run --net=host -t .../chromedriver:chromium /cd

... then access <http://localhost:9515/{path}>.

Audio is exposed by RTP, with destination IP address 224.0.0.56 (or
the address specified by -e WD_RTP_DEST=...) port 9515 (or the port
specified by -e WD_RTP_PORT=...).

* See also

ChromeDriver <https://sites.google.com/a/chromium.org/chromedriver/>.

ChromeDriver <https://code.google.com/p/selenium/wiki/ChromeDriver>.

The WebDriver Wire Protocol
<https://code.google.com/p/selenium/wiki/JsonWireProtocol>.

Similar Docker image for Firefox:
<https://github.com/wakaba/docker-firefoxdriver>.

* Author

Wakaba <wakaba@suikawiki.org>.

@nobuoka.

* Acknowledgements

Thanks to Hatena <http://hatenacorp.jp/>.
